# Fonts for JPG Export

Place a TTF font here (e.g. `DejaVuSans.ttf` or `Roboto-Regular.ttf`) for better typography in exported JPG images.

If no font is found here, the export will try system fonts (Arial, DejaVu Sans, Liberation Sans) and fall back to built-in fonts.
